---
- hosts: all
  gather_facts: false
  collections:
    - ansible.netcommon

  tasks:

    - name: Push RPM Telegraph Package on Arista
      ansible.netcommon.net_put:
        src: ./telegraf.rpm
        dest: /mnt/flash
        protocol: scp
